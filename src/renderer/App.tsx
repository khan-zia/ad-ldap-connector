import Layout from './components/Layout';
import BeginConfig from './pages/BeginConfig';

const App = (): JSX.Element => {
  return (
    <Layout>
      <BeginConfig />
    </Layout>
  );
};

export default App;
